import { motion } from "framer-motion";

const BlackOut = () => {
  return (
    <motion.div
      style={{
        backgroundColor: "black",
        height: "100%",
      }}
    ></motion.div>
  );
};

export default BlackOut;
